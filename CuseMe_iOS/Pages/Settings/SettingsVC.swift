//
//  SettingsVC.swift
//  CuseMe_iOS
//
//  Created by Yujin Shin on 2019/12/27.
//  Copyright © 2019 cuseme. All rights reserved.
//

import UIKit

class SettingsVC: UIViewController{
//,UITableViewDataSource, UITableViewDelegate {
/*
    @IBOutlet weak var settingsTV: UITableView!
    let cellId = "CustomCell"
    let content = ["비밀번호 변경", "보호자 연락처 변경"]
    let images = ["btTest01","btTest02"]
    override func viewDidLoad() {
        super.viewDidLoad()
        settingsTV.delegate = self
        settingsTV.dataSource = self
        
        self.settingsTV.register(UITableViewCell.self,forCellReuseIdentifier: cellId)
        
    }
    
    
    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
        return content.count
    }
    
  func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    let cell = settingsTV.dequeueReusableCell(withIdentifier: "CustomCell", for: indexPath as IndexPath) as! menusTVC
      
      cell.TVLabel.text = content[indexPath.row]
      cell.TVImage.image = UIImage(named: images[indexPath.row])
      
      return cell
  }
    */
    
    /*
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        let cell : UITableViewCell = self.settingsTV.dequeueReusableCell(withIdentifier: cellId, for: indexPath)
        
        
        //cell.menulabel.text = content[indexPath.row]
       
        //cell.iconImg.image = UIImage(named: images[indexPath.row])
        return cell

    

    */

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
 
